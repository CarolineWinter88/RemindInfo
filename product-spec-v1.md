# 不能断 —— v1 产品说明

> 状态：讨论稿。定位、数据模型、通知调度、补签卡机制已定案；末尾「待决问题」是仍需拍板的部分。

---

## 一、定位

**一个会在你快要断签时越催越凶、你一完成就立刻闭嘴的每日习惯管理器。**

平台 iOS，v1 纯本地、无后端、买断制。

它**不是**通用待办清单。所有设计都建立在两个前提上：事情是每日重复的，且用户会在 app 里点一下「完成」。没有完成信号，整套加压机制就退化成定时骚扰。

**竞品空档**：Streaks / Habitify / Done 都在做打卡，但提醒逻辑普遍是晚上发一条礼貌通知，发完就结束。没人把多邻国那套「随截止逼近逐级加压 + 完成即静音」做成通用工具。切口很窄，但确实空着。

---

## 二、三条核心机制

### 1. 时段聚合，不并行加压

一件事逐级催你有效；五件事各自逐级催你，用户在 22:45 收到四条语气都很重的通知，当场关闭通知权限。加压的威力来自稀缺性，并行即自毁。

所以：**同一时段内，不管几件事没做，只发一条聚合通知**，内容是「还有 3 件没完成」，点进去一次划完。升级的也是这一条链，不是每件事各升各的。

顺带解掉了通知配额问题（见第四节）。

### 2. 只有一个 streak

多个 streak 会互相稀释——五个七十几天的连胜，加起来不如一个七十天的有分量。

streak 衡量的是**「今天有没有守住」**，不是「今天有没有全做完」。达标线由用户自己设：我有五个习惯，我说每天做到两个就算数。谁顶谁不管，划得太松是自己的责任。

（这跟多邻国英语/音乐互顶是同一个思路：streak 衡量的是回访，不是进度。）

### 3. 完成路径必须极短

锁屏通知上左滑就能点完成，代码在后台跑一下把当天剩余的催促链全撤掉，不用打开 app。

如果完成必须开 app、找到条目、点一下，用户会懒得标记，然后被自己的 app 骂一顿——这是最快的卸载路径。

---

## 三、数据模型

```
Habit                          习惯
  id            UUID
  name          String
  emoji         String?
  slot          enum { morning, evening, night }    // v1 固定三档
  isCore        Bool            // 核心项不可被顶替；字段预留，v1 不暴露
  order         Int
  createdAt     Date
  archivedAt    Date?           // 软删除，保留历史记录

Completion                     完成记录
  id            UUID
  habitId       UUID
  dateKey       String          // "2026-09-09"，按 dayCutoffHour 计算，非自然日
  completedAt   Date
  source        enum { app, notificationAction, backfill }

DayRecord                      每日结算，日切时生成
  dateKey       String
  target        Int             // ★ 快照当天的达标线
  completedCount Int
  status        enum { pending, met, missed, frozen }

StreakState
  current               Int
  longest               Int
  lastMetDateKey        String
  lastSettledDateKey    String    // 惰性结算用，见第五节
  freezesRemaining      Int       // 上限 2
  progressToNextFreeze  Int       // 真实达标天数，满 10 送一张后归零

Settings
  dailyTarget    Int            // 达标线
  dayCutoffHour  Int            // 默认 3（凌晨 3 点前算前一天）
  slotTimes      [slot: Time]   // 默认 08:00 / 19:00 / 22:00
  intensity      enum { gentle, firm, relentless }
  quietHoursEnd  Time
```

**两个容易踩的点：**

- **`dateKey` 不按自然日切。** 默认凌晨 3 点。夜猫子 00:30 完成任务，心理上属于「今天」，按自然日算会给他一次莫名其妙的断签，这是不可原谅的体验。
- **`DayRecord.target` 必须快照。** 否则用户今天把达标线从 3 改成 1，历史上所有 missed 的日子会追溯性地变成 met，streak 凭空长出一截。历史必须冻结。

---

## 四、通知调度

### 核心原则：悲观排程 + 状态变化即撤销重排

iOS 的本地通知一旦 schedule 出去，内容和时间就固定了，系统在后台替你送达，**你的代码在触发那一刻没有机会运行**。所以「到点了先看看他做没做再决定要不要响」做不到。

唯一可行的形态是：按最坏情况（今天什么都没做）把通知排满，然后在用户完成时把不需要的撤掉。

### 每天排哪些通知

| 类型 | 数量 | 时间 |
|---|---|---|
| 时段通知 | 3 条 | slotTimes，语气平缓 |
| 睡前升级链 | 3 条 | 从 dayCutoff 回推，如 21:00 / 22:30 / 23:30，逐级加重 |

合计 **6 条/天**。

### 配额

iOS 一个 app 最多 64 条 pending 本地通知，超出的静默丢弃。

- 排程窗口 **7 天**：6 × 7 = 42 ≤ 64 ✓
- 剩余额度留作缓冲和兜底

用户长期不开 app 时提醒会断供——而恰恰是快要放弃的人最不会开 app。7 天窗口基本够用，要做得更稳最终得上服务端推送，那是 v2。

### 文案的精度差异

只有**今天**的通知可以写精确数量（「还剩 2 件」），因为未来那天会完成什么你不知道。第 1 天之后一律用通用文案（「今天还没有开始」）。这是硬限制，不是偷懒。

代价：**每完成一件事，当天剩余的通知都要 remove + 重新 add**，因为内容改了。如果嫌重排太频繁，可以退而求其次全用模糊文案，但精确计数带来的紧迫感值这个成本。

### 重排触发点（清单）

1. app 进入前台
2. 任一习惯被完成 / 取消完成（含通知 action 在后台执行）
3. **达标状态翻转** → 撤销当天所有剩余通知
4. 增删改习惯
5. 修改 slotTimes / dailyTarget / intensity
6. 日切（前台检测；BGAppRefreshTask 只作兜底，不可依赖）
7. 通知权限状态变化
8. 时区变化（`NSSystemTimeZoneDidChange`）

### 重排伪代码

```
func reschedule() {
    center.removeAllPendingNotificationRequests()

    for offset in 0..<7 {
        let day = dateKey(offset)

        // 今天已达标 → 今天剩下的一条都不排
        if offset == 0 && todayMet { continue }

        for slot in slots {
            guard time(day, slot) > now else { continue }
            let body = (offset == 0)
                ? "还有 \(pendingCount) 件没完成"
                : "今天还没有开始"
            schedule(day, slot, body, actions: actionsFor(pendingCount))
        }

        scheduleEscalationChain(day, intensity)
    }
}
```

### 撤销的四种情形

| 情形 | 行为 |
|---|---|
| 完成一件，仍未达标 | 全量重排，更新计数文案 |
| **达标瞬间** | 撤销当天全部剩余（含后续时段通知和整条升级链） |
| 取消完成（点错了） | 重排；若已过触发时间则补不回来，可接受 |
| 归档习惯 | 重排，可能直接达标 |

**第二行是整个产品最关键的一行代码。** 早上已经达标的人，晚上还被追杀，是最刺眼的那种 bug——它直接证明「这个 app 不知道我做了什么」，而这恰恰是它宣称的核心能力。

### 通知 action

- 只剩 1 件：`[完成] [稍后 30 分钟]`
- 剩多件：`[全部完成] [打开]`

后台 handler 要做：写 Completion → 更新 DayRecord → 判断达标 → 重排。handler 执行时间有限（约 30 秒），重排要快，别在里面做同步 I/O。

### 文案的语气梯度

名字定为「不能断」之后，全部文案有了统一的立场：**这是用户自己心里那句话，不是 app 在训斥他。** 产品的角色是帮他兑现决心，不是管教他。

五条原则：

1. **不指责。** 「你怎么又没做」「你答应过自己的」——这类句子一条都不要。用户已经知道自己没做，重复只会制造反感。
2. **越晚越短。** 早上可以是一句话，23:30 那条应该只剩数字和时间。那个时刻长句只会被直接划掉。
3. **连胜数字只在后段出现。** 早上就搬出「46 天」会让加压过早触顶，后面无处可升。它是最后的筹码，留到最后打。
4. **绝不提补签卡**（见第五节）。
5. **达标后当天不再出现任何文案**——靠撤销实现，不是靠措辞。

### 文案表

| 时机 | gentle | firm | relentless |
|---|---|---|---|
| 早 08:00 | 今天从这里开始 | 今天从这里开始 | 今天从这里开始 |
| 傍 19:00 | 今天还有 2 件 | 今天还有 2 件 | 今天还有 2 件 |
| 21:00 | — | 还有 3 小时，差 1 件 | 还有 3 小时，差 1 件 |
| 22:30 | 今天还差 1 件 | 46 天。还有 1 小时 | 46 天。还有 1 小时 |
| 23:30 | — | — | 最后 30 分钟。46 天 |

升级链条数：gentle 1 条 / firm 2 条 / relentless 3 条。默认 **firm**。最凶的那档必须用户主动选——不然第一周就有人给一星。

**v1.1 起还有一条降级规则**：Live Activity 运行期间，升级链一律压到只剩最后一条，避免倒计时和通知双重施压（见第六节）。

### 两条单独打磨的文案

**真断签的次日。** 不责备，把注意力立刻移到重启上：「45 天，停在昨天了。今天是第 1 天。」这是卸载率最高的瞬间，用平时的语气会很糟糕。

**补签卡消耗后。** 走 app 内提示，不走通知：「昨天用掉一张补签卡，46 天保住了。还剩 1 张。」平静陈述，不庆祝也不施压。

### 锁屏隐私

通知内容会出现在锁屏上，可能被旁人看到。「吃药」「记账」这类习惯名对一部分用户是敏感信息。

必须提供**「通知中不显示习惯名」**开关，打开后一律降级为「今天还有 2 件」。做成全局开关即可，v1 不必细到每个习惯。这个开关的存在本身也是信任信号，值得在引导里提一句。

这个开关在 v1.1 必须同时覆盖 Live Activity，且对它更保守——见第六节。

---

## 五、补签卡（streak freeze）

设计目标：让**损失从未真正发生过**，而不是先经历一次心跳骤停再挽回。手动使用要求用户在断签瞬间在场、意识到、并做出反应，而那个时刻他大概率正在睡觉。所以自动生效。

### 规则

| 项 | 规则 |
|---|---|
| 生效方式 | 自动。日切结算时未达标且有库存，消耗一张，`status = frozen` |
| 事前提示 | **无**。通知里绝不提「你还有卡」 |
| 事后告知 | **明确**。次日首次进前台时告知，日历上该天单独配色 |
| 连用限制 | 前一天是 frozen 则今天不可用，必须真实达标一天才解锁 |
| 库存上限 | 2 张 |
| 免费获取 | 每真实达标 10 天送 1 张 |
| 内购 | 只卖超出免费额度的部分 |
| 事后修复 | **不做** |

### 六条不能动摇的

**事前不提示。** 22:30 的通知若写「你还有 1 张补签卡」，当天动机立刻塌掉——反正有卡。

**事后必须告知，且在日历上可见。** 否则用户会觉得系统背着他消耗了他的东西。更重要的是，藏起来的话 streak 连用户自己都不信，约束力归零。

**连用限制保证语义。** 手里两张卡就能连着两天不做，streak 直接变成谎言。有这条限制，streak 至少还严格保证着「没有连续两天缺席」。

**必须有免费渠道。** 纯付费的补签卡会被骂卖焦虑，而且骂得有道理。10 天送 1 张还有个副作用：连胜越长的人越安全，而他们正是最不能丢的用户。

**不做事后修复。** 多邻国允许断签后花钱买回来，那个机制一旦存在 streak 就彻底失去意义——反正花钱能买。补签卡只做预防，只在库存内自动生效；真断了就是真断了。**这条线是前面所有加压设计的地基。**

**不在断签瞬间弹窗卖卡。** 那是转化率最高的时刻，也是最恶劣的。用户事后回想会觉得被算计，而整个产品建立在他信任你不会坑他之上。放在日结页面平静提供就够了。

### 真断签那一刻

不要显示一个刺眼的 0。显示「历史最长 61 天」，把成绩留住，给重新开始一点仪式感。

第二天那条通知的文案单独写——那是整个产品卸载率最高的瞬间，用平时的语气会很糟糕。

### 日切结算

```
func settle(day) {
    let rec = DayRecord(day)

    if rec.completedCount >= rec.target {
        rec.status = .met
        streak.current += 1
        streak.progressToNextFreeze += 1
        if streak.progressToNextFreeze >= 10 && streak.freezesRemaining < 2 {
            streak.freezesRemaining += 1
            streak.progressToNextFreeze = 0
        }
    }
    else if streak.freezesRemaining > 0 && DayRecord(day - 1).status != .frozen {
        rec.status = .frozen
        streak.freezesRemaining -= 1
        // streak.current 不变：不增不减
        pendingFreezeNotice = day        // 次日进前台时告知
    }
    else {
        rec.status = .missed
        streak.longest = max(streak.longest, streak.current)
        streak.current = 0
    }
    streak.lastSettledDateKey = day
}
```

**结算必须是惰性的。** 不能依赖 BGAppRefreshTask 在每天凌晨跑——它由系统随机调度，可能几天不执行。正确做法是每次进前台时，从 `lastSettledDateKey` 补算到昨天为止的所有未结算日期。用户消失五天后回来，五天的结算连同补签卡消耗要一次性正确算出，这段逻辑值得单独写测试。

### 连带影响

有了缓冲，用户敢把 `dailyTarget` 设得更高——没有缓冲时人会保守求安全。引导流程里可以利用这一点。

---

## 六、Live Activity 与灵动岛（v1.1）

### 为什么它比升级链更适合这个产品

升级链是**点状施压**：三条通知各出现一秒，划掉就没了。Live Activity 是**持续在场**——一个倒计时挂在锁屏上，从傍晚到日切，每次拿起手机都看见「还有 2 件 · 3:41:20」。环境压力比脉冲压力更贴合「不能断」的心理。

加上 iOS 17 起支持的交互按钮（App Intent），**完成按钮可以直接放在灵动岛和锁屏上**，一点即完成，连通知都不用等。这是第二节核心机制第 3 条的终极形态。

时间结构也巧合地吻合：Live Activity 活跃时长上限约 8 小时，之后在锁屏保留数小时过期状态（iOS 18 之后这个上限有放宽，具体数值要对着你的最低支持版本核一遍）。20:00 启动、03:00 日切正好 7 小时，卡在限制以内。

### 启动方式是硬约束

| 方式 | 要求 | 覆盖 |
|---|---|---|
| app 内启动 | **必须前台** | 仅当天打开过 app 的人 |
| push-to-start | iOS 17.2+，APNs + 服务端 | 全部用户 |

本地通知启动不了，后台也启动不了。

无后端时，只有当天打开过 app 的人晚上才会有倒计时——而这批人恰恰是最可能达标的。最需要被按住的是今天压根没想起这件事的人，对他们这个功能完全无效。

**所以：这是第一个真正值得为之建后端的功能。** 服务端推送的价值不止是补通知配额，push-to-start 才是它的主要用途。

### 与升级链的冲突（必须解决）

倒计时已经一直在锁屏上，再每隔一小时捅用户一下，不是加码，是烦人。

**规则：Live Activity 运行期间，升级链自动降到只剩最后一条**（临门那下），其余全部撤掉。持续压力由倒计时提供，通知只负责临界提醒。这条逻辑要写进重排引擎，否则两套机制会打架。

### 呈现设计

| 位置 | 内容 |
|---|---|
| 灵动岛 minimal | 剩余件数 |
| 灵动岛 compact | 左：件数　右：倒计时 |
| 灵动岛 expanded | 倒计时 + 未完成列表 + 「全部完成」按钮 |
| 锁屏 | 同 expanded，加连胜天数 |

灵动岛只有 iPhone 14 Pro 及以后机型有，而锁屏 Live Activity 覆盖 iOS 16.1+ 全机型。**设计对象是 Live Activity，灵动岛只是它的一种呈现**——不要为岛做设计。

### 实现要点

- 倒计时用 `Text(timerInterval:countsDown:)`，系统本地渲染、自动走秒，**不消耗更新配额**。只有件数变化时才真正更新一次 content state
- **达标瞬间 `activity.end(dismissalPolicy: .immediate)`**。这和第四节那条「最关键的一行代码」是同一规则的另一半——做完了倒计时还挂着，比通知没撤更刺眼，因为它一直在那儿
- 单次更新体积上限 4KB
- 用户可在系统设置里单独关闭 Live Activities，这是又一个权限漏斗

### 两个风险

**审核。** Live Activity 被拒最常见的理由是「不是真实的进行中事件」，被当成常驻广告位。你的用法有明确起止和倒计时，站得住，但提审描述要突出「当日截止倒计时」，而不是「提醒常驻」。

**隐私。** 它在锁屏停留数小时，曝光时间远长于通知。第四节的隐私开关必须覆盖 Live Activity，而且对它应当更保守：**默认只显示件数和倒计时，不显示习惯名**，想看点开。

---

## 七、v1 范围

### 砍掉

- 账号、后端、服务端推送
- 好友、社交、排行榜
- 复杂重复规则（隔天、每周几、每月）——只做每天，这是定位不是妥协
- 自定义时段数量（固定三档）
- 分组互顶（用 dailyTarget 代替，见第二节）
- 位置 / 日历 / 动作触发
- 统计图表（只留 streak 数字 + 最近 7 天点阵）
- Live Activity / 灵动岛、Widget、Apple Watch —— **v1.1 的三件事，优先级 Live Activity > Widget > Watch**（见第六节）
- 主题、自定义图标

### 不能砍（容易被误砍）

- **通知内完成按钮**：完成路径长 = 死
- **当天补签**：早些时候做了但忘了点，必须能补，否则用户会因为一次误伤而放弃
- **数据备份**：见下一节
- **补签卡的免费部分**：内购可以延后，但免费卡和自动生效逻辑必须在 v1，它是留存的安全网
- **`isCore` 字段**：v1 不暴露，但结构留好，否则以后加要迁数据

---

## 八、命名与 App Store 呈现

### 名称：不能断

选它的理由是**说话的人是谁**。「不许断」是 app 在命令用户，「不能断」是用户自己心里的那句话——是他的决心，app 只是帮他兑现。前者容易激起「你凭什么管我」的抵触，后者是用户主动认领的目标。

这个立场跟所有机制都对得上：补签卡、凌晨 3 点日切、达标线自定义，潜台词都是「我知道你不想断，我来帮你守住」，而不是「你敢断试试」。

避开了品类词也是有意的。「习惯」「打卡」「Habit」「Streak」「Daily」在两个 App Store 里都已饱和，而且描述的是分类不是产品。「不能断」不是常见短语，搜索区分度高，不会混进一堆备忘录和购物清单。

**已知的小瑕疵**：有轻微多义（网络不能断、电不能断），实际不影响理解，但 ASO 时可能混入少量无关流量。

### 上架名称

名称字段 30 字符、副标题 30 字符，ASO 权重主要在名称字段。所以实际形态是：

```
不能断 - 每日习惯打卡提醒
```

品牌名在前保辨识度，品类关键词在后接搜索流量。

### 英文名：Badger（待商标验证）

不建议直译。"Can't Break" 拗口，"Unbroken" 偏重。中英各按各的语感取名很正常，两边不必对应。

选 Badger 的理由是**一词两义**：动词是「不停纠缠、催促某人」，名词是獾，语义和图标一次到位。这种词不多。

它在英式英语里带轻微负面色彩，比 hound 更贴近「烦」——对这个产品其实是加分，用户下载前就知道它会烦你。但引导文案要把这层意思转正：**是你请它来烦你的。**

图标上獾也比猎犬有优势：黑白条纹的头部辨识度高，缩到锁屏那个尺寸依然认得出，而犬类头像做小了基本长一个样。

**⚠️ 未验证，不要据此开始设计资产。** 上一个候选 Hound 已因此出局——SoundHound（上市公司）有同名语音助手 app，2016 年上架，Android 端超百万下载，产品页至今在线。第 9 类商标大概率在其名下且活跃使用，且 App Store 搜索会被它完全压住。

Badger 同样没查。定案前必须自己跑完：
- USPTO TESS 检索，第 9 类、第 42 类
- 美区 App Store 实搜 "Badger"，看有没有体量大的占位者
- 中国商标网（若要注册中文名对应的英文）

这两件事十分钟能做完，比事后改名便宜太多。

### 图标

**比名字更值得早想。** 这个 app 大部分时间是以通知形式出现在锁屏上的，那个圆角小方块的辨识度，比它在搜索结果里的排名更影响用户会不会顺手划掉。

獾的黑白条纹在极小尺寸下依然成立，这是选它的实际理由之一。灵动岛的 minimal 状态尺寸更小（见第六节），图标方案要拿那个尺寸去验，不能只看 App Store 里的大图。

### 上架前必须查

- 商标：第 9 类、第 42 类（中文名「不能断」与英文名 Badger 各查一遍）
- 两个 App Store 搜同名与近音
- **国区**：名称需对得上软著登记和 ICP 备案信息，这一步现在卡得比较严，名字定晚了会拖工期

英文名的检索**不能等到上架前**——图标、Live Activity 的呈现、App Store 截图都建立在名字之上，改名意味着这些资产全部重做。先查再画。

---

## 九、待决问题

### 1. 数据备份 —— 我认为不能算在「v1 无后端」里

streak 是这个产品的全部价值。用户换手机、误删 app、丢了 80 天连胜，不是流失一个用户，是流失一个最忠诚的用户，而且会去 App Store 骂。

三个选项，成本递增：
- 手动导出 / 导入 JSON —— 几乎零成本，但没人会主动导出
- iCloud Key-Value Store —— 存不下完整历史，只能存 streak 摘要
- **CoreData/SwiftData + CloudKit** —— 真正解决问题，但会占掉 v1 相当一部分工期

倾向第三个。这可能是 v1 唯一值得的「重」投入。

### 2. 补签卡的发放节奏（机制已定，见第五节，参数未定）

「10 天送 1 张、上限 2 张」是拍脑袋定的，需要用真实数据校准。

- 发得太松：streak 失去压迫感，且没人会买
- 发得太紧：安全网形同虚设，回到直接归零

上线后该看的是 **frozen 天数占比**。健康区间大概在 3%–8%：低于 3% 说明卡几乎没用上，高于 8% 说明产品对用户来说太难了，那不是卡的问题，是达标线引导有问题。

另一个未定项是**真断签次日那条通知的文案**。这是卸载率最高的瞬间，值得单独打磨，也值得 A/B。

### 3. 达标线可以随时改 = 可以作弊

用户 23:50 发现今天来不及了，把 target 从 3 改成 1，直接达标。

建议：**调低次日生效，调高立即生效。** 需要在 UI 上说清楚，否则用户会以为是 bug。

### 4. 权限漏斗

通知权限拒绝了，这个 app 就没有任何价值。

考虑 **provisional authorization**（`UNAuthorizationOptions.provisional`）：无需弹窗即可授权，通知静默送达通知中心，用户在真实感受到价值后再决定是否升级为横幅。对这个 app 的冷启动特别合适。代价是前几天的提醒不打扰用户，也就不起作用——需要在引导里说明。

### 5. 有效性怎么度量

留存指标是表象。真正该盯的：
- **升级链的边际效果**：第 2 条、第 3 条通知分别挽回了多少个当天达标？如果第 3 条几乎没用，就该砍掉，它只是在制造反感
- streak 中位数、断签后 7 天回归率

这套数据也是决定要不要做「智能时机」的依据。

### 6. 定价

买断 + 补签卡内购。订阅制在这个品类阻力极大，用户潜意识觉得提醒不配按月付费。Due 靠「忽略之后死缠烂打」这一个点卖了十几年，14.99 买断，值得认真拆。

### 7. 时区与旅行

跨时区飞行时 dateKey 怎么算？按本地时间会导致某一天被压缩或拉长。建议：**按本地时间，且当天永不判 missed**，宁可放过。

### 8. 伦理上的一条线

多邻国的连胜制造了大量真实焦虑，很多人为了保 streak 去刷一分钟垃圾课程，学习本身没有发生。你要把这台机器套到任意事情上，可能造出一个用户又依赖又讨厌的东西——而讨厌会转化成卸载。

至少做到：**用户随时能一键关掉升级链，且这个开关不要藏。**

### 9. 冷启动获客

红海，对手是免费且在变强的系统提醒事项 + Siri。而「智能加压」是个**不可见的功能**——做对了用户感觉不到，没法在 App Store 截图里展示。

这对一个靠自然流量的买断 app 是硬伤。截图和文案要怎么把「它会在 23:30 逼你」这件事可视化，是需要单独想的问题，不能等到快上架才开始。

---

## 附：建议的实现顺序

1. 数据模型 + 完成/取消 + 惰性日切结算 + 补签卡消耗（无通知，纯逻辑跑通）
2. 结算与重排的单元测试 —— 这两处是隐蔽 bug 的重灾区，尤其是「用户消失 N 天后回来」的补算路径
3. 重排引擎
4. 时段通知 → 升级链 → 通知 action → 文案表与锁屏隐私开关
5. 达标撤销（第四节表格第二行）
6. 日历视图（frozen 天的可见性是补签卡可信度的前提）
7. CloudKit 同步
8. 引导流程 + 权限漏斗
9. 补签卡内购

**v1.1**

10. Live Activity（app 内启动版，无后端）+ 升级链降级规则
11. Widget
12. 服务端 + push-to-start，把 Live Activity 覆盖到没打开过 app 的人 —— 这一步才是它真正生效的时候
